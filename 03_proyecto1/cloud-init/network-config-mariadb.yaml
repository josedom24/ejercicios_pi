network:
  version: 2
  ethernets:
    ens3:
      dhcp4: true                        # red-externa con DHCP (acceso exterior)
    ens4:
      dhcp4: false
      addresses: ["192.168.201.2/24"]    # IP estática en red-conf
    ens5:
      dhcp4: false
      addresses: ["10.0.0.2/24"]         # IP estática en red-datos
